import { Fragment } from "react";
import './Logo_Heading.css';

export const Heading = () => {
    return(
        <Fragment>
            <span className="heading">People Manager</span>
        </Fragment>
    );
};